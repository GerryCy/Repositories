<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--mapper为根元素，namespace指定了命名空间-->
<mapper namespace="cn.test.ssm.mapper.ProductDemo">

    <!--实现一个简单的列表查询的功能（使用动态sql和sql片段便于扩展，虽然这只是个小的demo并没有做其他的扩展，但是可以养成一种习惯）-->

    <!--sql片段+动态sql-->
    <sql id="queryListCondition">
        <where>
            <if test="productExtend != null">
                <if test="productExtend.name != null and productExtend.name != ''">
                    product.pname LIKE '%${productExtend.name}%'
                </if>
            </if>
        </where>
    </sql>

    <!--为了便于扩展，使用ProductExtent类作为输出映射，这样除了可以查询Product之外还可以扩展其他的字段-->
    <select id="findProductListByName" parameterType="cn.test.ssm.po.ProductQueryVo" resultType="cn.test.ssm.po.ProductExtend">
        SELECT product.* FROM product
        <include refid="queryListCondition"></include>
    </select>

    <select id="queryProductInfo" parameterType="java.lang.Integer" resultType="cn.test.ssm.po.ProductExtend">
        SELECT pname,shop_price FROM product WHERE pid = #{id}
    </select>
</mapper>
